# Asa-Hopkins.github.io
A website for me to dump esoteric knowledge in a less formal way

The intitial HTML and CSS is from https://github.com/setetres/evenbettermotherfuckingwebsite, which is GPLv3 so this website must also be GPLv3.

Mostly as a note to self, here's the commands I use for compressing images:
`pngquant --quality=3 --nofs --posterize 3 --ext .png --force [file]`
`oxipng -o max -s -z [file]`